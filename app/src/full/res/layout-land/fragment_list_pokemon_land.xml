<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/my_color_light"
    tools:ignore="MissingDefaultResource">

    <!-- Layout da lista de pokémons -->
    <include
        layout="@layout/fragment_list_pokemon"
        android:id="@+id/layout_include_list"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toStartOf="@id/scroll"
        app:layout_constraintWidth_percent="1.0"
        android:visibility="visible"/>

    <!-- ScrollView para o detalhe do Pokémon -->
    <ScrollView
        android:id="@+id/scroll"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        app:layout_constraintStart_toEndOf="@id/layout_include_list"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        android:visibility="gone"
        >

        <FrameLayout
                android:id="@+id/layout_include_detail"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:visibility="gone"
                tools:visibility="visible"/>

    </ScrollView>

</androidx.constraintlayout.widget.ConstraintLayout>
